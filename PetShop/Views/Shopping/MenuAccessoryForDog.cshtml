@model List<PetShop.Models.Category>

<ul class="list-group list-group-flush">
    <li href="#" class="list-group-item p-0 text-white text-uppercase font-weight-bold rounded bg-success pl-2">
        <img src="images/dog_collar_64px.png" alt="" class="img-menu-type"> phụ kiện
    </li>
    @foreach (var item in Model)
    {
        <li>
            <a href="#" class="list-group-item bg-transparent text-white border-0">@item.Name</a>
        </li>
    }

</ul>
